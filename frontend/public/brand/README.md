Place the custom background image for the game screen here:

`frontend/public/brand/crash-background.png`

The app already references it from `src/styles/app.css` through `--brand-background-image`.
If the file is absent, the dark neon fallback background remains visible.
